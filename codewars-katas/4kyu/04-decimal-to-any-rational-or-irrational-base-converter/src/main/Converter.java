public class Converter {

	public static String converter(double n, int decimals, double base) {

    // decimals >= 0
    // 2 <= base <= 36 (1 not needed)
    // 36 Base of numeration 0-Z ; "A(for11*base [base > 10]).Z02" is 'possible'
    // --> Always prioritize using the most significant digit that is possible!

    // output formated with "XXXX.decimals" (0 if < 1)
    return "";
  }
}
